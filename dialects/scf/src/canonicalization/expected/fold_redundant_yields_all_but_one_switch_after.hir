builtin.function public extern("C") @fold_redundant_yields_all_but_one_switch(%0: u32) -> u32 {
    %8 = "test.constant"() <{ value = !builtin.number<44> }> : () -> u32;
    %5 = "test.constant"() <{ value = !builtin.number<11> }> : () -> u32;
    %6 = "test.constant"() <{ value = !builtin.number<22> }> : () -> u32;
    %7 = "test.constant"() <{ value = !builtin.number<33> }> : () -> u32;
    %14 = scf.index_switch %0 <{ cases = !builtin.array_u32<1, 2, 3> }> default_region {
        scf.yield %8;
    };
    %9 = "test.add"(%5, %14) <{ overflow = !builtin.overflow<checked> }> : (u32, u32) -> u32;
    %10 = "test.add"(%9, %7) <{ overflow = !builtin.overflow<checked> }> : (u32, u32) -> u32;
    builtin.ret %10;
};