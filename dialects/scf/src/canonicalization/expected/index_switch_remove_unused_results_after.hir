builtin.function public extern("C") @index_switch_remove_unused_results(%0: u32) -> u32 {
    %2 = test.constant 2 : u32;
    %4 = test.constant 4 : u32;
    %7 = scf.index_switch %0
    case 1 {
        scf.yield %2 : (u32);
    }
    default {
        scf.yield %4 : (u32);
    } : (u32);
    builtin.ret %7 : (u32);
};
