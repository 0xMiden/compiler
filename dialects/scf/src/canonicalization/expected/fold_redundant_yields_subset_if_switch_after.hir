builtin.function public extern("C") @fold_redundant_yields_subset_if_switch(%0: u32) -> u32 {
    %8 = "test.constant"() <{ value = !builtin.number<44> }> : () -> u32;
    %7 = "test.constant"() <{ value = !builtin.number<33> }> : () -> u32;
    %9 = "test.constant"() <{ value = !builtin.number<55> }> : () -> u32;
    %6 = "test.constant"() <{ value = !builtin.number<22> }> : () -> u32;
    %3 = "test.constant"() <{ value = !builtin.number<11> }> : () -> u32;
    %4 = "test.constant"() <{ value = !builtin.number<0> }> : () -> u32;
    %5 = "test.eq"(%0, %4) : (u32, u32) -> i1;
    %17 = scf.if %5 then {
        scf.yield %6;
    } else {
        %19 = scf.index_switch %0 <{ cases = !builtin.array_u32<1, 2> }> default_region {
            scf.yield %9;
        };
        scf.yield %19;
    } : (u32);
    %10 = "test.add"(%3, %17) <{ overflow = !builtin.overflow<checked> }> : (u32, u32) -> u32;
    %11 = "test.add"(%10, %3) <{ overflow = !builtin.overflow<checked> }> : (u32, u32) -> u32;
    builtin.ret %11;
};