builtin.function public extern("C") @fold_redundant_yields_different_pos_switch(%0: u32) -> u32 {
    %3 = "test.constant"() <{ value = !builtin.number<11> }> : () -> u32;
    %4 = "test.constant"() <{ value = !builtin.number<22> }> : () -> u32;
    %8, %9 = scf.index_switch %0 <{ cases = !builtin.array_u32<1, 2> }> default_region {
        scf.yield %3, %4;
    };
    %5 = "test.add"(%8, %9) <{ overflow = !builtin.overflow<checked> }> : (u32, u32) -> u32;
    builtin.ret %5;
};