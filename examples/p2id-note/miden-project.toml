[package]
name = "p2id"
version = "0.1.0"

[lib]
kind = "note"
namespace = "miden::p2id::p2id"
path = "src/lib.rs"

[profile.release]
debug = true

[dependencies]
miden-core = "*"
miden-protocol = "*"
basic-wallet = { path = "../basic-wallet" }
