[package]
name = "counter-note"
version = "0.1.0"

[lib]
kind = "note"
namespace = "miden::counter_note::counter_note"
path = "src/lib.rs"

[profile.release]
debug = true

[dependencies]
miden-core = "*"
miden-protocol = "*"
counter-contract = { path = "../counter-contract" }
