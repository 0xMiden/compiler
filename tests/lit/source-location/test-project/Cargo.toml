cargo-features = ["trim-paths"]

[package]
name = "source_location_test"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[profile.release]
debug = true
trim-paths = ["diagnostics", "object"]

[profile.dev]
debug = true
trim-paths = ["diagnostics", "object"]
