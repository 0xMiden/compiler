[package]
name = "cross-ctx-note"
version = "0.1.0"

[lib]
kind = "note"
namespace = "miden::cross_ctx_note::cross_ctx_note"
path = "src/lib.rs"

[dependencies]
cross-ctx-account = { path = "../cross-ctx-account" }
