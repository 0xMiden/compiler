[package]
name = "swapp-note"
version = "0.1.0"

[lib]
kind = "note"
namespace = "miden::swapp_note::swapp_note"
path = "src/lib.rs"

[dependencies]
basic-wallet = { path = "../../../../examples/basic-wallet" }
