cargo-features = ["trim-paths"]

[package]
name = "issue-invalid-stack-offset-movup"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
miden = { path = "../../../../sdk/sdk" }

[package.metadata.component]
package = "miden:issue-invalid-stack-offset-movup"

[package.metadata.miden]
project-kind = "note-script"

[profile.release]
trim-paths = ["diagnostics", "object"]

[profile.dev]
trim-paths = ["diagnostics", "object"]
