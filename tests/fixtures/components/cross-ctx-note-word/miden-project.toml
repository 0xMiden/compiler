[package]
name = "cross-ctx-note-word"
version = "0.1.0"

[lib]
kind = "note"
namespace = "miden::cross_ctx_note_word::cross_ctx_note_word"
path = "src/lib.rs"

[dependencies]
cross-ctx-account-word = { path = "../cross-ctx-account-word" }
