[package]
name = "cross-ctx-note-word"
version = "0.1.0"

[lib]
kind = "note"

[dependencies]
cross-ctx-account-word = { path = "../cross-ctx-account-word" }

[package.metadata.dependencies]
cross-ctx-account-word = { path = "../cross-ctx-account-word/wit/cross-ctx-account-word.wit" }
