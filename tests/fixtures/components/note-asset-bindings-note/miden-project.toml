[package]
name = "note-asset-bindings-note"
version = "0.1.0"

[lib]
kind = "note"
namespace = "miden::note_asset_bindings_note::note_asset_bindings_note"
path = "src/lib.rs"

[dependencies]
basic-wallet = { path = "../../../../examples/basic-wallet" }
