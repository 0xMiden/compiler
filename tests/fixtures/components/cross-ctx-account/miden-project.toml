[package]
name = "cross-ctx-account"
version = "0.1.0"

[lib]
kind = "account-component"
namespace = "miden::cross_ctx_account::foo"
path = "src/lib.rs"

[package.metadata.miden]
supported-types = ["RegularAccountUpdatableCode"]
