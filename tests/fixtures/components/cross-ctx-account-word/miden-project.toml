[package]
name = "cross-ctx-account-word"
version = "0.1.0"

[lib]
kind = "account-component"
namespace = "miden:cross-ctx-account-word/foo@1.0.0"

[package.metadata.miden]
supported-types = ["RegularAccountUpdatableCode"]
