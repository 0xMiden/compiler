[package]
name = "{{crate_name}}"
version = "0.1.0"

[lib]
path = "src/lib.rs"
kind = "tx-script"
namespace = "miden:base/transaction-script@1.0.0"

[dependencies]
miden-core = "*"
miden-protocol = "*"
# TODO: Add your account contract dependencies here
add-contract = { path = "../add-contract" }
