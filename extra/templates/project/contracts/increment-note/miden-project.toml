[package]
name = "increment-note"
version = "0.1.0"

[lib]
path = "src/lib.rs"
kind = "note"
# Names the note's exported procedures, e.g. `miden::increment_note::increment_note::run`.
namespace = "miden::increment_note::increment_note"

[dependencies]
miden-core = "*"
miden-protocol = "*"
counter-account = { path = "../counter-account" }
